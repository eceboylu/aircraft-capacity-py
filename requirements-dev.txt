# Development / test dependencies. Production kurulumuna DAHIL EDİLMEZ.
#
# Kurulum:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Sürümler mevcut çalışan environment'tan doğrulanmıştır
# (venv/Scripts/pip list --format=freeze).
-r requirements.txt

pytest==9.1.1
