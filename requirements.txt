# Production runtime dependencies.
#
# SQLAlchemy: ORM/engine katmanı - app/models.py, app/queue/models.py,
# app/db.py ve tüm queue modülleri tarafından import edilir.
# Sürüm, mevcut çalışan environment'tan doğrulanmıştır
# (venv/Scripts/pip list --format=freeze).
SQLAlchemy==2.0.52

# MySQL sürücüsü - app/db.py'nin production notu
# ("mysql+pymysql://user:pass@host:3306/dbname") PyMySQL bekliyor.
# SQLite kullanan development/test ortamlarında GEREKMEZ (Python'un
# yerleşik sqlite3 modülü yeterlidir) ama production MySQL hedefi için
# zorunludur. Sürüm PyPI'daki güncel kararlı sürümdür (pip index
# versions PyMySQL ile doğrulandı).
PyMySQL==1.2.0

# ADIM (Operational-Day Scope) - stdlib `zoneinfo`'nun IANA timezone
# veritabanı. Linux/macOS'ta sistem genelde bunu zaten sağlar, ama bu
# Windows geliştirme ortamında `zoneinfo.ZoneInfo("Europe/Istanbul")`
# `tzdata` KURULU DEĞİLKEN `ZoneInfoNotFoundError` fırlatıyor (bu ADIM
# sırasında DOĞRUDAN doğrulandı) - `Airport.timezone`'daki GERÇEK IANA
# adlarını (flight_airports.sql) yorumlayabilmek için gerekli. Uydurma
# bir offset/mapping DEĞİL - Python'un resmi tzdata paketi.
tzdata==2026.4

# Database Migration Strategy - schema DEĞİŞİKLİKLERİ artık alembic/versions/
# altındaki explicit migration dosyalarıyla yönetiliyor (bkz. alembic/README).
# Çalışan app kodu (worker/web/pipeline) bunu import ETMEZ - bu, `alembic`
# CLI'ının ayrı, explicit bir deploy adımı olarak (`alembic upgrade head`)
# çalıştırılması için gereken bir araçtır, runtime dependency değildir.
alembic==1.20.0
